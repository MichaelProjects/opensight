FROM python:3.9

WORKDIR /app

ADD requirements.txt requirements.txt
RUN pip install --upgrade pip && pip install -r requirements.txt 

ADD generator.py generator.py
ADD app_conf.json app_conf.json
CMD ["python3", "generator.py"]
