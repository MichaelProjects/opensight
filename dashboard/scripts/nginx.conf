server {
        listen   80;
        server_name  0.0.0.0;

        access_log  /var/log/nginx/localhost.access.log;

        location / {
                root   /var/www/dashboard;
                index  index.html index.htm;
        }
}